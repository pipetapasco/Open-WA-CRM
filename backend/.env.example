# --- Django ---
DEBUG=true
SECRET_KEY=django-insecure-tu-clave-secreta-aqui
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,backend

# --- Base de Datos (Postgres) ---
# Estas variables son usadas tanto por Django como por la imagen de Postgres
POSTGRES_DB=wadb
POSTGRES_USER=wauser
POSTGRES_PASSWORD=wapassword
POSTGRES_HOST=db
POSTGRES_PORT=5432

# URL de conexión para Django (debe coincidir con lo de arriba)
DATABASE_URL=postgres://wauser:wapassword@db:5432/wadb

# --- Redis & Celery ---
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=django-db
REDIS_URL=redis://redis:6379/0

# --- WhatsApp API (Déjalos vacíos por ahora) ---
WHATSAPP_API_URL=https://graph.facebook.com/v17.0/
WHATSAPP_API_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=